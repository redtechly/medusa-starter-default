version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: backend:starter
    container_name: medusa-server-default
    environment:
      DATABASE_URL: postgres://admin:yIeXp3ImbRDZqowWM2iqE15KGeakcjp4@z4r7y2.stackhero-network.com:5432/admin?sslmode=require
      NODE_ENV: development
      JWT_SECRET: something
      COOKIE_SECRET: something
      PORT: 9000
    ports:
      - "9000:9000"
    volumes:
      - .:/app/medusa
      - node_modules:/app/medusa/node_modules

volumes:
  node_modules:
